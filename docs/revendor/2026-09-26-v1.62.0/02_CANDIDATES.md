# Candidates (AbsorbTracker)

None. v1.62.0 adds no public member, descriptor field or row field. It moves code between files
(OptionsIds, OptionsIdList, OptionsRegistry, OptionsCombat) and ships kit revisions 28 to 31.
Everything in it reaches the addon on the copy (class A):

- the `None.` line under an empty "Functions `lizard` warned on" table in `RESULTS.md` (kit
  revision 30, `ATS-20`);
- generated files left out of the band table (kit revision 31, `ATS-21`). This addon declares no
  `Kit.layoutCap.exempt` set, so its table is unchanged.

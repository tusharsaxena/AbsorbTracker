# Summary (AbsorbTracker)

LibKa0s v1.61.0 -> v1.62.0 from the tag, base taken from the CLAUDE.md provenance line. Options 25 -> 26,
OptionsWidgets 31 -> 32, OptionsTabs 5 -> 6, and four new files at minor 1 (OptionsRegistry,
OptionsIds, OptionsIdList, OptionsCombat). `tests/_kit` moves from kit revision 27 to 31. The
CLAUDE.md provenance line is rolled. No span bundle, no base correction, no contract blocker, nothing
adopted, nothing declined.

Reached the addon on the copy: the `None.` line under an empty lizard watch-list table (`ATS-20`) and
the generated-file carve-out in the band table (`ATS-21`).

Host-side: the stub comment in `settings/OptionsSetup.lua`, the Options tripwire in
`tests/test_ltrap.lua` (now reads every `Options*.lua` off the XML), and the library census in three
docs. `docs/test-cases.md` regenerated with no change.

Gate after the copy:

- tests: 810 passed, 0 failed, 0 skipped, 810 total (810 before)
- luacheck: 0 warnings / 0 errors in 66 files
- lizard: no function above CCN 15
